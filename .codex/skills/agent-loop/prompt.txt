Read @agent-loop-instructions.md and follow it. Your assigned issue is #{ISSUE_ID}; its title is in $AGENT_LOOP_ISSUE_TITLE and its description is in $AGENT_LOOP_ISSUE_BODY. `gh` is disabled for the worker, so read the issue from those environment variables rather than calling it. Implement, run focused validation, refactor, and create a local commit. Do not push, open a pull request, or invoke hosted AI reviewers; after the worker exits, the wrapper opens a draft PR and owns convergent local Codex/Claude review through inline findings, fixes, replies, and resolutions. If you bail, write the local classification record required by the instructions and exit nonzero; do not attempt GitHub labels/comments because `gh` is disabled for the worker.
