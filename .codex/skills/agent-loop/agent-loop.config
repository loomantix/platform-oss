# Consumer-owned agent-loop settings. Literal `key = value` lines; this file is
# parsed, never sourced. Do not embed credentials, identifiers, PHI, or user data.

# Integration branch. Leave empty to use origin/HEAD, then main.
base_branch =

# Run once inside each new issue worktree. Use an isolated frozen install; never
# symlink mutable node_modules or equivalent dependency directories. It must not
# commit, switch branches, or leave Git-visible worktree changes.
setup_hook =

# Required. Run after the worker, after every local review pass, and after the
# fresh-base merge. It must not commit, switch branches, or leave worktree
# changes. Prefer targeted checks and avoid unbounded full-suite output.
validation_hook =

# Required acknowledgement of the current review-hook contract. Existing
# consumer configs must migrate the hooks below before adding this marker.
review_contract_version = 4
config_doctor = true
# Required for contract-v4 auto mode. The pinned launcher owns Claude's `low`
# effort policy; the doctor verifies the launcher's contract query.
claude_effort_policy = low

# Contract v4 pins both review hooks to the upstream-owned trusted launcher. The
# wrapper runs Codex first, then Claude, and
# repeats only when either hook commits a material fix. Minor-only fixes are
# validated and retained without restarting; convergence requires a complete
# Codex-then-Claude round with no material fixes. The launcher runs each engine
# from an empty session root, materializes engine-native guidance from immutable
# base-commit blobs, and exposes the issue worktree only as the review target.
# Each wrapper opens a draft PR before either hook. Each hook must read all prior PR
# review threads, post every confirmed finding inline before editing, collect
# the pass's fixes into one committed candidate, then call
# `$AGENT_LOOP_REVIEW_PUSH_HELPER` exactly once. The helper runs the configured
# validation hook before publishing that candidate. The reviewer then replies,
# resolves, and fails if any valid finding or local-review thread remains. Hooks
# may use `gh` only for that draft PR review contract; the cooperative command
# guard rejects direct `git push`, but is not same-UID or credential isolation. They must
# not mutate the base branch, other issues/PRs, or invoke hosted reviewers.
# Scope both hooks against `$AGENT_LOOP_REVIEW_BASE_SHA`; it is immutable for the
# round, while `$AGENT_LOOP_REVIEW_BASE` is the fully qualified fetched remote
# ref. The wrapper rejects consumer hook overrides so this review-routing
# contract cannot be configured away.
# Hooks also receive `$AGENT_LOOP_PR_NUMBER`, `$AGENT_LOOP_PR_URL`, and
# `$AGENT_LOOP_PR_HEAD_SHA`. The last value is the exact head at pass start.
# Every successfully completed clean or changed hook must call `review-ledger.js
# write-result` to create the canonical `$AGENT_LOOP_REVIEW_RESULT_FILE`; reviewers
# never copy fingerprint arrays. A blocked hook must use the deterministic
# `review-ledger.js write-blocked-result` command documented by deepcritique and
# return without claiming a clean
# or changed pass. Hooks use
# the deterministic ledger helper for findings and the resumable `dispose`
# transaction, but never post pass/completion markers; the wrapper validates the
# result and posts those attestations. Later hooks and final validation must not
# create, remove, or alter an accepted result file.
claude_review_hook = "$AGENT_LOOP_CODEX_REVIEW_LAUNCHER" --engine claude
codex_review_hook = "$AGENT_LOOP_CODEX_REVIEW_LAUNCHER" --engine codex
review_max_rounds = 4
# Whole review budget across every engine and round. Resume reloads the
# deadline recorded in run state, so it never grants another full window.
# Legacy run state predating that field has no recorded deadline: resume
# derives one from the state file's last checkpoint time instead, which does
# extend the window. Size this value against the recorded-deadline case.
review_timeout_seconds = 7200

# Optional worker override. When empty, the wrapper runs `codex exec` with the
# prompt from prompt.txt. The default worker honors the model settings below.
worker_hook =
worker_model =
worker_fallback_model =
worker_retries = 1
# Both timeout values must be positive; GNU `timeout 0` would disable the bound.
worker_timeout_seconds = 3600
hook_timeout_seconds = 3600
retry_on_timeout = true
retry_delay_seconds = 15

# `ready` preserves legacy open/closed dependency behavior. `merged-to-base`
# requires dependency PR merge commits to be present in origin/<base>.
dependency_gate = ready

branch_prefix = agent-loop
worktree_root = /tmp/agent-loop-worktrees
log_root = /tmp/agent-loop-logs
log_max_kb = 1024
output_max_lines = 40
